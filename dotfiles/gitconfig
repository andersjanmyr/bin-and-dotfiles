[user]
	name = Anders Janmyr
  email = anders@janmyr.com
[apply]
  whitespace = nowarn
[color]
  status = auto
  diff = auto
  branch = auto
[github]
  user = andersjanmyr
  token = 084236cc4e86c10b200beeb0bede88dc
[alias]
  amend = commit --amend -C HEAD
  br = branch
  ci = commit
  co = checkout
  conflicts = !git ls-files -u | cut -f 2 | sort -u  
  dump = cat-file -p
  fd = rev-list -n 1 HEAD --
  hist = log --pretty='format:%Cblue%h%d%Creset %s %Cgreen%an%Creset, %ar' --graph
  ld = log --diff-filter=D --summary
  m = merge --no-ff
  st = status
  sta = stash
  stat = status
  type = cat-file -t
  untracked = ls-files -o -X.gitignore --exclude=.*

[core]
  excludesfile = /Users/andersjanmyr/.gitignore
  autocrlf = input
[heroku]
	account = anders
[push]
	default = matching
