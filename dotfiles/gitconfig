[user]
	name = Anders Janmyr
  email = anders@janmyr.com
[apply]
  whitespace = nowarn
[color]
  status = auto
  diff = auto
  branch = auto
[github]
  user = andersjanmyr
  token = 084236cc4e86c10b200beeb0bede88dc
[alias]
  co = checkout
  st = status
  stat = status
  br = branch
  ci = commit
  sta = stash
  untracked = ls-files -o -X.gitignore --exclude=.*
  amend = commit --amend -C HEAD
  hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
  type = cat-file -t
  dump = cat-file -p
  fd = rev-list -n 1 HEAD --
  ld = log --diff-filter=D --summary
  conflicts = !git ls-files -u | cut -f 2 | sort -u  

[core]
  excludesfile = /Users/andersjanmyr/.gitignore
  autocrlf = input
[heroku]
	account = anders
[mergetool "mvimdiff3"]
	cmd = mvim -f -d -c \"wincmd J\" \"$MERGED\" \"$LOCAL\" \"$BASE\" \"$REMOTE\"
[merge]
	tool = mvimdiff3
