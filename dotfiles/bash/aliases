# Command Enhancements

alias kindle='sendgmail -t anders.janmyr@free.kindle.com -s Convert'
alias tree=redwood

# MySql
alias mysqlstart='sudo /usr/local/Cellar/mysql/5.1.49/share/mysql/mysql.server start'
alias mysqlstop='sudo /usr/local/Cellar/mysql/5.1.49/share/mysql/mysql.server stop'

# Postgres
alias pgstart='pg_ctl -D /usr/local/var/postgres -l /usr/local/var/postgres/server.log start'
alias pgstop='pg_ctl -D /usr/local/var/postgres stop -s -m fast'

# MongoDB
alias mongostart='mongod run --config /usr/local/Cellar/mongodb/1.8.1-x86_64/mongod.conf'

# Utility
alias reload='source ~/.bashrc'
alias eb='v  ~/bin'

alias less='less -FRSX'

# Common -- Some are from Damian Conway
alias a='ls -A' # -A all except literal . ..
alias la="ls -A -l -G"
alias c='clear'
alias cl='clear; l'
function cdc() {
    cd $1; ls
}
alias cls='clear; ls'
alias h='history'
alias l.='ls -d .[^.]*'
alias l='ls -lhGt'  # -l long listing, most recent first
                    # -G color
alias lh="ls -lh"
alias ll='ls -lhG'  # -l long listing, human readable, no group info
alias lt='ls -lt' # sort with recently modified first

alias md='mkdir -p'
alias s='cd ..'   # up one dir

function mcd() {
    mkdir -p "$1"
    cd "$1"
}

function pd() {
  dir=`find ~ -name "*$1*" -type d -maxdepth 3 | head -1`
  if [ "$dir" != '' ]; then
    cd "$dir"
  else
    echo "Cannot find project dir with name: $1"
  fi
}

alias k9="killall -9"
function killnamed () {
    ps ax | grep -i $1 | cut -d ' ' -f 1 | xargs kill
}
function zipr() {
  zip -r $1.zip $1
}

# Finder
alias o='open . &'
alias ff='open -a Firefox'

# General code


# Processes
alias tu='top -o cpu' # cpu
alias tm='top -o vsize' # memory


# Git
alias ungit="find . -name '.git' -exec rm -rf {} \;"
alias ga='git add'
alias gai='git add -i'
alias gaa='git add --all'
alias gau='git add --update'
alias gb='git branch'
alias gba='git branch -a'
alias gc='git commit -v'
alias gca='git commit -v -a'
# Commit pending changes and quote all args as message
function gg() {
    git commit -v -m "$*"
}
function gga() {
    git commit -v -am "$*"
}
alias gco='git checkout'
alias gd='git diff'
alias gds='git diff --staged'
alias gdm='git diff | mate'
alias gl='git hist'
alias glo='git log --pretty=oneline'
alias gmf='git merge --ff-only'
alias gm='git merge --no-ff'
alias gnp="git log origin/master..master"
alias gpf='git pull --ff-only'
alias g='git status -sb'
alias vg='v .git/config'
# Git clone from GitHub
function gch() {
  git clone git://github.com/$USER/$1.git
}

# Setup a tracking branch from [remote] [branch_name]
function gbt() {
  git branch --track $2 $1/$2 && git checkout $2
}

# Call from inside an initialized Git repo, with the name of the repo.
function new-git() {
  ssh git@example.com "mkdir $1.git && cd $1.git && git --bare init"
  git remote add origin git@example.com:$1.git
  git push origin master
  git config branch.master.remote origin
  git config branch.master.merge refs/heads/master
  git config push.default current
}


# Ruby
alias r="rails"

# Rails
alias rdbm="bundle exec rake db:migrate"
alias rdbr="bundle exec rake db:reset"
alias be="bundle exec"


# TDD / BDD

alias wat="watchr ~/bin/watchr/rails.rb"

# Syntax check Javascript
function jsc() {
  jsl -conf /etc/jsl/jsl.conf -process $1
}

function aiff2mp3() {
  lame -h -V 0 $1.aif $1.mp3
}
function wav2mp3() {
  lame -h -V 0 $1.wav $1.mp3
}


alias jsdoc='jsdoc -a -p -t=/usr/local/Cellar/jsdoc-toolkit/2.3.2/libexec/jsdoc-toolkit/templates/jsdoc'

alias tweet='v ~/DropBox/work/tweet.txt'
alias tweet='v ~/DropBox/work/tweet.txt'

alias flushhosts='sudo dscacheutil -flushcache'


