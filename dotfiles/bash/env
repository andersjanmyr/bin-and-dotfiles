# export JAVA_HOME="`/usr/libexec/java_home -v '2.8*'`"
#export JAVA_HOME=/Library/Java/Home # System Java
export JAVA_HOME=/usr/lib/jvm/java-6-sun

export PATH=$HOME/bin:$HOME/Projects/lms-env/bin:$HOME/Projects/docker-utils:/usr/local/bin:$PATH:/usr/local/sbin:$SCALA_HOME/bin

export EDITOR="$HOME/bin/v -f"

export CLICOLOR=1

# Don't use this version of rake-completion, too slow
# source /etc/bash_completion.d/rake-completion.sh

export UDS_HOME=~/tmp

export PGDATA=/usr/local/var/postgres

export NODE_PATH=/usr/local/lib/node:/usr/local/lib/node_modules:/usr/local/lib/jsctags
export NODE_ENV=development

export HISTIGNORE="&:[a-z]:[a-z][a-z]:[a-z][a-z][a-z]"

export SHOWOFF_EVAL_RUBY=1

# Rubinius
export RBXOPT='-X19'

export AWS_CONFIG_FILE=/home/CORPUSERS/23053085/.aws/janmyr.config


# Ruby performance patches
export RUBY_GC_HEAP_INIT_SLOTS=1000000
export RUBY_GC_HEAP_SLOTS_INCREMENT=1000000
export RUBY_GC_HEAP_SLOTS_GROWTH_FACTOR=1
export RUBY_GC_MALLOC_LIMIT=1000000000
export RUBY_GC_HEAP_FREE_MIN=500000

# Git prompt config
export GIT_PS1_SHOWDIRTYSTATE=true
export GIT_PS1_SHOWUNTRACKEDFILES=true 
export GIT_PS1_SHOWUPSTREAM="auto"
# export GIT_PS1_SHOWSTASHSTATE=true


# Postgres psql
export HISTFILESIZE=
export HISTSIZE=
export PAGER=less

if [ -f /etc/bash_completion ]; then
. /etc/bash_completion
fi

if `type brew 2> /dev/null`; then
  if [ -f `brew --prefix`/etc/bash_completion ]; then
    . `brew --prefix`/etc/bash_completion
  fi
  export PYTHONPATH=$(brew --prefix)/lib/python2.7/site-packages
  PATH="$(brew --prefix josegonzalez/php/php54)/bin:$PATH"
  # MAVEN
  export M2_HOME=`brew --prefix maven`/libexec
fi


# Locale settings for aws command
# http://docs.aws.amazon.com/cli/latest/tutorial/getting_started.html
export LANG="en_US.UTF-8"
export LC_COLLATE="en_US.UTF-8"
export LC_CTYPE="en_US.UTF-8"
export LC_MESSAGES="en_US.UTF-8"
export LC_MONETARY="en_US.UTF-8"
export LC_NUMERIC="en_US.UTF-8"
export LC_TIME="en_US.UTF-8"
export LC_ALL=

export GOPATH=$HOME/gocode
export PATH=$PATH:/usr/local/go/bin:$GOPATH/bin

[ -s $HOME/.nvm/nvm.sh ] && . $HOME/.nvm/nvm.sh # This loads NVM

# RVM
if [[ -s ~/.rvm/scripts/rvm ]] ; then
        source ~/.rvm/scripts/rvm ;
fi
[[ -s $rvm_path/scripts/completion ]] && source $rvm_path/scripts/completion


